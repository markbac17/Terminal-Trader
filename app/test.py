from unittest import TestCase
from app import Trade

class TestCase(TestCase):
    
    def 
    
    (self, parameter_list):
        pass
    def test_trade(self):
        trade =Trade(volume=30)
        self.assertIsInstance(trade,Trade)
        self.assertEqual(trade.volume,  Trade)
        self.assertIsNone(trade)
